{
  "name": "@jahed/promises",
  "version": "0.1.2",
  "description": "Functions for Promises",
  "main": "lib/index.js",
  "module": "es/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "es",
    "docs"
  ],
  "sideEffects": false,
  "author": "Jahed Ahmed",
  "license": "MIT",
  "repository": "https://github.com/jahed/promises",
  "scripts": {
    "lint": "tslint",
    "test": "jest",
    "build:lib": "rimraf lib && tsc",
    "build:es": "rimraf es && tsc --outDir ./es/ --target es6 --module es6",
    "build:docs": "rimraf docs && typedoc --theme default --out docs --mode file",
    "push:git": "git push && git push --tags",
    "push:docs": "bash -i ./publish-docs.sh",
    "prepublishOnly": "yarn lint && yarn test && yarn build:lib && yarn build:es && yarn build:docs",
    "postpublish": "yarn push:git && yarn push:docs"
  },
  "devDependencies": {
    "@types/jest": "23.3.1",
    "@types/node": "10.5.4",
    "jest": "23.4.2",
    "rimraf": "2.6.2",
    "ts-jest": "23.0.1",
    "tslint": "5.11.0",
    "tslint-config-standard": "7.1.0",
    "typedoc": "0.11.1",
    "typescript": "2.9.2"
  }
}
