{
  "name": "@jahed/promises",
  "version": "0.0.5",
  "description": "Functions for Promises",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "docs"
  ],
  "author": "Jahed Ahmed",
  "license": "MIT",
  "scripts": {
    "lint": "tslint --project ./tsconfig.json",
    "test": "jest",
    "build:lib": "rimraf lib && tsc",
    "build:docs": "rimraf docs && typedoc --theme default --exclude '**/*.test.ts' --out docs --mode file --target ES6",
    "push:git": "git push && git push --tags",
    "push:docs": "bash -i ./publish-docs.sh",
    "prepublishOnly": "yarn lint && yarn test && standard-version && yarn build:lib && yarn build:docs && yarn push:git && yarn push:docs"
  },
  "devDependencies": {
    "@types/jest": "23.3.0",
    "@types/node": "10.5.3",
    "jest": "23.4.1",
    "rimraf": "2.6.2",
    "standard-version": "4.4.0",
    "ts-jest": "23.0.1",
    "tslint": "5.11.0",
    "tslint-config-standard": "7.1.0",
    "typedoc": "0.11.1",
    "typescript": "2.9.2"
  }
}
